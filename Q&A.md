# 学习问答记录

学习过程中提出的问题与解答，按主题分组，方便回顾。

> 记录格式：
> ```
> ### 问题简述（YYYY-MM-DD）
> **问**：……
> **答**：……（解答要点，可附代码片段或图示链接）
> ```

## 通用问题

### 在 macOS 上可以进行安卓开发吗（2026-09-18）
**问**：macOS 上能做安卓开发吗？
**答**：可以。安装 Android Studio（区分 Intel / Apple Silicon 版本）即可获得 SDK、模拟器、ADB 全套工具；真机调试开启 USB 调试直连，无需驱动。M 系列芯片模拟器性能很好。

### 能用 Docker 编译安卓吗（2026-09-18）
**问**：安卓项目能在 Docker 里编译吗？
**答**：编译 APK/AAB 可以（JDK + Android SDK + Gradle 装进 Linux 容器即可，常用于 CI 保证环境一致）；但 Docker 里**不能跑安卓模拟器**（macOS 的 Docker 无 KVM），GUI 调试仍需本机或真机。

### 为什么教程用原生 WebGL 而不用 Three.js（2026-09-20）
**问**：用 Three.js 写示例代码是不是更方便？
**答**：写代码更方便，但学不到东西。Three.js 把渲染管线、着色器、MVP 矩阵、光照模型全部封装成黑盒，而这些正是本教程的教学目标。入门阶段（1~15 课）坚持用原生 WebGL；大纲末尾的第 16 课是 Three.js 对照课，届时再体会框架的价值。

### 网页上只能看到画面，看不到代码怎么办（2026-09-20）
**问**：课程网页打开后只有渲染结果，看不到代码和注释，没办法学习。
**答**：已改进——现在每课页面分为上下两部分：上方画布是运行效果，下方直接内嵌显示本课 `main.js` 完整源码（语法高亮 + 行号），讲解注释都在代码里，对照着读即可。实现方式是 `common/code-view.js` 在页面加载时读取同目录的 `main.js` 渲染出来，所以源码永远只有一份、不会和页面显示脱节。注意：直接双击 HTML 用 `file://` 打开时浏览器禁止网页读取本地文件，此时会显示一个源码链接代替；用 `python3 -m http.server` 或访问线上地址时正常显示。

### 什么是参数化，图形学为什么离不开它（2026-09-21）
**问**：可以讲讲参数化算法吗？
**答**：参数化是用自由参数表示几何形状的方法：曲线用一个参数（圆：`x=r·cos t, y=r·sin t`，`t∈[0,2π)`），曲面用两个（球面用 `(θ, φ)`）。相比隐式方程（`x²+y²=r²` 只能判断点是否在圆上），参数式能直接**枚举**出形状上的点，正好契合 GPU「顶点缓冲存采样点、光栅化连线成图」的工作方式；对参数求导还能方便地得到切线和法线（光照课要用）。常见例子：贝塞尔曲线 `B(t)=(1-t)²P₀+2(1-t)t·P₁+t²P₂`（设计软件钢笔工具）、球面网格（双重循环采样，第 06/07 课）、纹理 UV 坐标（本质是曲面参数化，第 04 课）。两个要点：① 曲线永远是折线近似，采样越密越光滑；② attribute 不一定存位置，也可以存参数 `t`、在顶点着色器里算坐标，几何定义就搬进了着色器。已在第 01 课「动手试试」中加入参数方程画圆的练习。

### 有没有可以在线改代码做练习的地方（2026-09-21）
**问**：「动手试试」的练习能直接在网页里做吗？改完代码能立刻看到效果吗？
**答**：新增了 playground/ 练习场（首页有入口横幅，课程页画布下方也有跳转链接）：左侧选题目，中间编辑器改代码，右侧 iframe 自动刷新运行结果；运行出错会在画面顶部提示第几行。每道题都预置了可直接运行的模板，任务写在模板开头注释里。编辑器是 CDN 引入的 CodeMirror——工具页允许第三方库，但课程示例仍然零依赖（原生 WebGL 就是教学目标）；CDN 加载失败时会降级为纯文本框。注意模板文件是 fetch 加载的，双击 file:// 打开会提示改用 `python3 -m http.server` 或访问线上地址。

<!-- 新课程相关的问题按下方分组追加 -->

## 第 01 课 Hello Triangle

### 顶点着色器和片元着色器分别做什么，为什么要分开（2026-09-20）
**问**：顶点着色器和片元着色器的功能是什么？为什么要区分成两个阶段？
**答**：顶点着色器对**每个顶点**运行一次，决定顶点画在屏幕哪里（输出 `gl_Position`）；片元着色器对三角形覆盖的**每个像素**运行一次，决定该像素的颜色（输出 `gl_FragColor`）；两者之间的「光栅化」由硬件自动完成，根据顶点位置算出三角形盖住哪些像素。分开的原因：① 运行次数差几个数量级——一个三角形只有 3 个顶点，却可能覆盖几十万个像素，分开后 GPU 可以用成千上万个核心并行处理这些互不依赖的像素；② 职责不同——一个管「形状/位置」，一个管「颜色/外观」，换颜色不用动几何逻辑；③ 光栅化这个固定硬件阶段天然把两种计算隔开。类比：顶点着色器钉钉子定角，光栅化填砖，片元着色器给每块砖上色。相关解释已补充进第 01 课 `main.js` 开头注释。

### 两种着色器的输入分别是什么（2026-09-21）
**问**：顶点着色器和片元着色器的输入参数是什么？
**答**：GLSL 的 `main()` 没有形参，输入输出靠特殊修饰的全局变量。顶点着色器的输入：`attribute`（JS 经缓冲区传入的每顶点数据，如 `a_position`）和 `uniform`（一次绘制中所有顶点共享的全局值，如 `u_scale`）。片元着色器的输入：`varying`（顶点着色器输出经光栅化插值后的值，第 03 课讲）、`uniform`（与顶点着色器看到的是同一份，如 `u_color`）和内置变量 `gl_FragCoord`（当前像素在画布上的坐标，首页背景动画用的就是它）。关键不对称：片元着色器**没有** attribute，不能直接读缓冲区，顶点数据只能经 varying 插值间接到达。输出方面：顶点着色器写 `gl_Position`，片元着色器写 `gl_FragColor`。已补入第 01 课 `main.js` 开头注释。

### gl_Position 和 varying 是什么关系（2026-09-21）
**问**：顶点着色器的输出既说是 `gl_Position` 又说 varying，两者是什么关系？
**答**：两者都是顶点着色器的输出，分工不同，由光栅化联系起来。`gl_Position` 是必写的特殊输出，管「形状」：光栅化根据三个顶点的位置算出三角形覆盖哪些像素，以及每个像素受三个顶点的影响比例（插值权重）。varying 是可选的自定义输出，管「数据」：想传给片元着色器的值（颜色、UV 等），光栅化用同一组权重把各顶点的 varying 值平滑混合到每个像素上。一句话：`gl_Position` 决定插值的权重，varying 是被插值的货物。`gl_Position` 本身不会到达片元着色器（像素坐标读内置变量 `gl_FragCoord`）；varying 可有可无，01/02 课都没用，第 03 课的渐变三角形就是靠它把三个顶点的红绿蓝混合到中间像素。已补入第 01 课注释。

### 还有哪些内置的输入输出变量（2026-09-21）
**问**：除了 attribute / uniform / varying 和 gl_Position / gl_FragColor，还有别的输入输出变量吗？
**答**：自定义的输入输出永远只有 attribute / uniform / varying 三种，其余都是 GPU 自动提供的内置变量：顶点着色器另有输出 `gl_PointSize`（画 gl.POINTS 时点的大小）；片元着色器另有输入 `gl_FrontFacing`（像素是否属于正面三角形，第 06 课背面剔除用到）和 `gl_PointCoord`（片元在点内的 [0,1] 坐标，画圆点/粒子用）；扩展/进阶输出 `gl_FragData`（多渲染目标，和第 12 课帧缓冲相关）与 WebGL2 的 `gl_FragDepth`（手动写深度）；WebGL2 还新增顶点输入 `gl_VertexID` / `gl_InstanceID`（顶点/实例自动编号，第 15 课讲）。已补入第 01 课注释。

## 第 02 课 Uniforms 与动画

（暂无）
